# Part 4: Context-Free Languages and Pushdown Automata




<!-- 第二章：上下文无关语言 (Context-Free Languages) 核心总结这一章主要介绍了描述语言的两种更强大的工具：上下文无关文法 (CFG) 和 下推自动机 (PDA)。相比于正则语言，它们能处理具有递归结构的语言（如编程语言的语法）。以下是本章的基础定义、核心定理串联，以及关键的习题推荐。0. 基础定义 (Basic Definitions)在深入定理之前，必须明确本章最核心的两个概念。上下文无关文法 (Context-Free Grammar, CFG)定义 2.2：一个上下文无关文法是一个 4 元组 $(V, \Sigma, R, S)$，其中：$V$ 是一个有限集合，称为变元 (Variables)。$\Sigma$ 是一个有限集合，称为终结符 (Terminals)，与 $V$ 不相交。$R$ 是一个有限集合，称为规则 (Rules)。每条规则由一个变元和一个箭头组成，箭头右边是变元和终结符的字符串（即 $A \rightarrow w$，其中 $A \in V, w \in (V \cup \Sigma)^*$）。$S \in V$ 是起始变元 (Start Variable)。推导 (Derivation)：如果 $u, v, w$ 是变元和终结符的字符串，且 $A \rightarrow w$ 是一条规则，则称 $uAv$ 生成 $uwv$，写作 $uAv \Rightarrow uwv$。如果可以通过一系列步骤从起始变元推导出字符串 $w$（即 $S \Rightarrow \dots \Rightarrow w$），则称 $w$ 由该文法生成。上下文无关语言 (Context-Free Language, CFL)任何能由某个上下文无关文法 (CFG) 生成的语言，被称为上下文无关语言 (CFL)。即：$L(G) = \{w \in \Sigma^* \mid S \Rightarrow^* w\}$。1. 核心定理梳理与逻辑串联第一部分：表达与标准形式 (Representation & Normal Forms)定理 2.9：乔姆斯基范式 (Chomsky Normal Form, CNF)内容：任何上下文无关语言 (CFL) 都可以由一个乔姆斯基范式的上下文无关文法 (CFG) 生成。形式：规则只能是 $A \rightarrow BC$ 或 $A \rightarrow a$ 的形式（允许 $S \rightarrow \epsilon$）。重要性/串联：这是CFG的一种简化标准形式。串联：它是证明 泵引理 (Theorem 2.34) 的基础工具，因为规范的树结构使得计算树的高度和路径长度变得容易。第二部分：计算模型等价性 (Equivalence) —— 本章的核心桥梁定理 2.20：CFG 与 PDA 的等价性内容：一个语言是上下文无关的，当且仅当有一个下推自动机 (PDA) 识别它。分解：引理 2.21 (Forward)：如果一个语言是 CFL (有文法)，那么存在一个 PDA 识别它。引理 2.27 (Reverse)：如果一个 PDA 识别某个语言，那么它是 CFL (有文法)。重要性/串联：这个定理打通了生成器 (Grammar) 和 识别器 (Automaton) 之间的壁垒。它类似于第一章中“正则表达式 $\iff$ 有穷自动机”的关系。它让你在证明一个语言是 CFL 时，可以自由选择是写文法还是画 PDA。推论 2.32：正则语言是上下文无关语言的子集内容：每个正则语言都是上下文无关的。串联：PDA 实际上就是“DFA + 栈”。如果不利用栈，PDA 就退化为 NFA。第三部分：局限性与反证法 (Limitations)定理 2.34：上下文无关语言的泵引理 (Pumping Lemma for CFLs)内容：如果 $A$ 是 CFL，则存在泵长度 $p$，任何长度至少为 $p$ 的字符串 $s \in A$ 都可以被分割成五部分 $s = uvxyz$，满足：$uv^ixy^iz \in A$ (对于 $i \ge 0$)$|vy| > 0$ (v 和 y 不同时为空)$|vxy| \le p$重要性/串联：这是证明某个语言不是 CFL 的主要工具。对比正则语言的泵引理（切成三份 $xyz$），CFL 切成五份（$uvxyz$），体现了 CFL 解析树中递归（两个变量重复出现）的特性。第四部分：确定性 (Determinism) —— 进阶内容 (Section 2.4)这一部分探讨了 CFL 中更实用的子集：确定性上下文无关语言 (DCFL)，这对编译器设计至关重要。定理 2.42 (DCFL 的补运算封闭)：DCFL 类在补运算下封闭（注意：普通 CFL 在补运算下不封闭）。定理 2.52 (DK-Test)：一个 CFG 是确定性的，当且仅当它通过 DK 测试（检查自动机状态中的点规则）。定理 2.57 (DPDA 与 DCFG 的等价性)：一个（带结束标记的）语言能由 DCFG 生成，当且仅当它是 DCFL。定理 2.66 (LR(k) 与 DCFL)：一个（带结束标记的）语言能由 LR(1) 文法生成，当且仅当它是 DCFL。串联：这部分建立了 DPDA、DCFG 和 LR(k) 文法之间的等价关系，说明了为什么 LR 解析器在编译器中如此重要。2. 逻辑串联图解建议按以下逻辑复习：定义：理解 CFG 和 CFL 的基本构造（0. 基础定义）。标准化：Theorem 2.9 (CNF) 让 CFG 变得规范。等价模型：Theorem 2.20 引入了 PDA，证明 PDA 与 CFG 等价。边界：Theorem 2.34 (Pumping Lemma) 划清了 CFL 的界限（哪些语言超出了 CFL 的能力）。子集：Section 2.4 深入探讨了 PDA 的确定性版本 (DPDA)，它对应 DCFL，具有特殊的封闭性质。3. 重要的典型习题 (Important Exercises & Problems)整理这些习题可以帮你掌握核心考点：A. 证明非上下文无关 (Pumping Lemma 的应用)Example 2.36: 证明 $B = \{a^n b^n c^n \mid n \ge 0\}$ 不是 CFL。（经典题目，必看）Example 2.38: 证明 $D = \{ww \mid w \in \{0,1\}^*\}$ 不是 CFL。（不仅是非正则，也不是 CFL）Problem 2.30: 练习使用泵引理证明以下语言不是 CFL：(a) $\{0^n 1^n 0^n 1^n \mid n \ge 0\}$(b) $\{0^n \# 0^{2n} \# 0^{3n} \mid n \ge 0\}$B. 上下文无关语言的封闭性 (Closure Properties)Exercise 2.2:利用 $A = \{a^m b^n c^n\}$ 和 $B = \{a^n b^n c^m\}$ 证明 CFL 在 交集 (Intersection) 运算下不封闭（$A \cap B = \{a^n b^n c^n\}$，这不是 CFL）。利用德摩根定律证明 CFL 在 补集 (Complementation) 运算下不封闭。Exercise 2.16: 证明 CFL 在 并集 (Union)、连接 (Concatenation) 和 星号 (Star) 运算下是封闭的。C. 歧义性 (Ambiguity)Exercise 2.8: 给出一个句子在文法中有两个不同最左推导的例子，并解释其不同的语义。Problem 2.29: 证明语言 $\{a^i b^j c^k \mid i=j \text{ 或 } j=k\}$ 是 先天歧义的 (Inherently Ambiguous)。这说明有些语言无论怎么写文法，都是有歧义的。D. 构造题 (Construction)Exercise 2.4 / 2.6: 练习为各种语言构造 CFG。这是基本功。Exercise 2.14: 练习将一个 CFG 转化为 乔姆斯基范式 (CNF)。需要熟练掌握消除 $\epsilon$-规则、单一规则 (unit rules) 的步骤。 -->
