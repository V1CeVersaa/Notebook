# Advanced Topics

## Lecture 17: RL Theory Basics 

## Lecture 18: Variational Inference

概率模型用来表示一个概率分布，给定随机变量 $x$，概率模型使用一个分布 $p(x)$ 描述之，概率模型既可以是边缘分布 $p(x)$，也可以是条件分布 $p(x \mid y)$。

我们需要了解的是隐变量模型/Latent Variable Model，形式上来说，其是在**证据**与**查询**之外引入一个额外的变量的概率模型，对于 $p(x)$，我们的查询是 $x$，没有证据，对于 $p(y \mid x)$，我们的证据是 $x$，查询是 $y$。由于我们引入了一个既不是证据又不是查询的量，那么这个变量就需要在求出目标概率时被边缘化。

一个经典的模型是使用混合模型来拟合 $p(x)$，设数据呈现为三个明显簇，但我们并不知道每个点属于哪个簇。此时可令 $z$ 为一个三类的离散分类变量，每一个簇对应一个高斯分布，这样就可以得到 $p(x) = \sum_{z=1}^3 p(x \mid z) p(z)$。

对于条件概率也有类似的分解，比如 $p(y \mid x) = \sum_{z} p(y \mid x, z) p(z \mid x)$。如果 $z$ 的概率依赖于 $x$，那么 $p(y \mid x) = \sum_{z} p(y \mid x, z) p(z \mid x)$ 的分解也是合理的。

更一般地来讲，我们可以将隐变量模型看作是使用简单的先验分布 $p(z)$ 与简单的条件分布 $p(x \mid z)$ 组合，从而得到复杂的 $p(x) = \displaystyle\int p(x \mid z) p(z) \mathrm{d}z$。

至于如何训练隐变量模型，我们可以使用最大似然估计来训练，如果我们有模型 $p_\theta(x)$ 和数据 $\mathcal{D} = \{x_i\}_{i=1}^N$，那么最大似然估计给出下面更新：

$$
\theta \leftarrow \operatorname*{\arg\max}_{\theta} \frac{1}{N}\sum_{i=1}^N \log p_\theta(x_i).
$$

将 $p(x) = \displaystyle\int p(x \mid z) p(z) \mathrm{d}z$ 代入，我们得到：

$$
\theta \leftarrow \operatorname*{\arg\max}_{\theta} \frac{1}{N}\sum_{i=1}^N \log \left( \int p_\theta(x_i \mid z) p(z) \mathrm{d}z \right).
$$

但是这种方法数值性质非常差，很难计算，如果 $z$ 是连续的，那么我们需要计算积分，如果 $z$ 是离散的，那么这样的情形是为数不多还好计算的情形。因此我们需要一种方式来估计对数似然，解决方法就是期望对数似然，使用的式子为：

$$
\theta \leftarrow \operatorname*{\arg\max}_{\theta} \frac{1}{N}\sum_{i=1}^N \mathbb{E}_{z \sim p_\theta(z \mid x_i)} \left[ \log p_\theta(x_i, z) \right].
$$


<!--

直觉是：如果我们“猜”出与 \(x_i\) 最匹配的 z，并把它当作真标签去做 MLE，就得到“完全数据”的对数似然；但更合理的做法是对 z 的不确定性进行加权平均，于是对 \(\log p_\theta(x_i, z)\) 按后验分布取期望。严格的合理性将在后续讲解变分推理时给出。

为什么这样更可处理？因为期望可以用采样来估计。对每个样本 \(x_i\)，采样 \(z_i^{(m)} \sim p_\theta(z \mid x_i)\)（m=1,…,M），有无偏的蒙特卡罗估计：
$$
\widehat{\mathcal{L}}_{\text{E-LL}}(\theta)
\;=\;
\frac{1}{N}\sum_{i=1}^N \;\frac{1}{M}\sum_{m=1}^M \log p_\theta\big(x_i, z_i^{(m)}\big).
$$
类似地，我们可以用采样来近似该目标的梯度，就像策略梯度那样用期望的梯度估计替代难以处理的精确积分。需要注意的是，这里关键的一步是要能从后验 \(p_\theta(z \mid x_i)\) 进行采样，或至少能对其进行有效近似。一旦我们能得到 \(p_\theta(z \mid x_i)\)，上述目标与梯度的估计就是可行的；而真正的难点恰恰在于如何计算或近似这个后验，这将是下一部分（变分近似与摊销变分推理）要解决的问题。

在直观层面，你可以将“估计 \(p_\theta(z \mid x)\)”理解为：把观测 x“反推”成潜变量 z 的一个分布（往往相当复杂），然后在这个分布下计算期望对数似然。下一讲我们将以变分推理为核心工具，系统地给出这一过程的可行算法，并解释其与深度网络（摊销推理）的结合方式。

## 总结

- 隐变量模型通过组合“简单”的先验 \(p(z)\) 与条件分布 \(p_\theta(x \mid z)\) 来表达复杂的边缘分布 \(p_\theta(x)\)，在条件建模 \(p_\theta(y \mid x)\) 中同理成立。  
- 在强化学习中，隐变量模型既可用于多模态策略，也可用于基于模型的潜状态时序建模；在逆强化学习与探索中也频繁出现。  
- 直接最大化 \(\log p_\theta(x)\) 困难在于对潜变量 z 的积分（或求和）与梯度计算常常不可 tractable。  
- “期望对数似然”采用对 \(\log p_\theta(x,z)\) 的后验期望，允许用采样进行无偏估计，类似策略梯度的思想；但核心挑战转移为如何有效获得或近似后验 \(p_\theta(z \mid x)\)。  
- 变分推理与摊销变分推理正是为此而生：它们提供了可扩展、可微分的后验近似方案，下一部分我们将正式进入这些方法的推导与实践。
 -->
